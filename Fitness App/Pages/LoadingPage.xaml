<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:skia="clr-namespace:Fitness_App.UI.Skia"
             x:Class="Fitness_App.Pages.LoadingPage"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False"
             BackgroundColor="#0B0F19">

    <!-- Session gate: brand + animated runner while auth/session resolves. -->
    <Grid VerticalOptions="Fill" HorizontalOptions="Fill">
        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="20">
            <Label Text="StrideX"
                   FontSize="42"
                   FontAttributes="Bold"
                   TextColor="#FC5200"
                   HorizontalOptions="Center" />
            <skia:RunnerLoadingView HorizontalOptions="Center"
                                    AccentColor="#FC5200"
                                    HeightRequest="160"
                                    WidthRequest="160" />
            <Label Text="Loading your session..."
                   FontSize="14"
                   TextColor="#94A3B8"
                   HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
